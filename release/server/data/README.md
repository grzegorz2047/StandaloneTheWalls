# Runtime data

This directory is intentionally empty in the release archive. The dedicated
server creates `identity.sqlite` here on first start. A verified registry cache
may later be stored as `registry.sfrb`.

Do not publish, commit, or attach this directory to bug reports.
