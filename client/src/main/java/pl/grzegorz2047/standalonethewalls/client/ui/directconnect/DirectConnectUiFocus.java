package pl.grzegorz2047.standalonethewalls.client.ui.directconnect;

/** Keyboard and pointer focus targets shared by the renderer and headless state machine. */
public enum DirectConnectUiFocus {
    ENDPOINT,
    HANDLE,
    TEAM_RED,
    TEAM_BLUE,
    TEAM_GREEN,
    TEAM_YELLOW,
    READY_ACTION,
    PRIMARY_ACTION,
    SECONDARY_ACTION
}
