/**
 * Server-owned integration from authenticated TLS player sessions through identity policy into a
 * bounded pre-lobby handoff queue.
 */
package pl.grzegorz2047.standalonethewalls.server.identity.session;
