package pl.grzegorz2047.standalonethewalls.server.administration.identity;

/** Independent capabilities for local identity administration. */
public enum IdentityAdministrationPermission {
    VIEW_IDENTITY,
    MANAGE_HANDLE_BINDINGS,
    MANAGE_PLAYER_BANS,
    MANAGE_REGISTRY
}
