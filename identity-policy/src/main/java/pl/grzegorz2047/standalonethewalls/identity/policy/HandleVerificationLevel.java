package pl.grzegorz2047.standalonethewalls.identity.policy;

/** Presentation status attached only to accepted handle authorizations. */
public enum HandleVerificationLevel {
    GLOBAL_VERIFIED,
    LOCAL_UNVERIFIED
}
