package pl.grzegorz2047.standalonethewalls.identity.policy;

/** Administrative mutation represented by one local player-ban audit event. */
public enum LocalPlayerBanAuditAction {
    BAN,
    UNBAN
}
