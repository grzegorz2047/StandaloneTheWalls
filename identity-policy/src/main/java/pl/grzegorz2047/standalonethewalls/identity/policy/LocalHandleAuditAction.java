package pl.grzegorz2047.standalonethewalls.identity.policy;

/** Administrative mutation represented by one local identity audit event. */
public enum LocalHandleAuditAction {
    RESERVE,
    UNBIND,
    REBIND
}
